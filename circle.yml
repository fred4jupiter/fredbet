machine:
  java:
    version: oraclejdk8
  services:
    - docker
  timezone:
    Europe/Berlin

dependencies:
  override:    
    - docker info
        
test:
  override:
  - mvn verify  
  post:
  - git config user.name "fred4jupiter"
  - git config user.email "hamsterhase@gmx.de"

deployment:
  hub:
    branch: master
    commands:
      - ./release.sh
